let currentIndex = 0; const cards = document.querySelectorAll(".event-card"), totalCards = cards.length; function showCard(r) { cards.forEach(r => { r.classList.remove("active") }), cards[r].classList.add("active") } function nextCard() { showCard(currentIndex = (currentIndex + 1) % totalCards) } function prevCard() { showCard(currentIndex = (currentIndex - 1 + totalCards) % totalCards) } cards[currentIndex].classList.add("active");