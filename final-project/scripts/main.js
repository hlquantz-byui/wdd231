import { setupNavigationToggle } from "./nav.js"; import { loadHomepageRecipes } from "./home.js"; import { loadAllRecipes } from "./recipe-gallery.js"; document.addEventListener("DOMContentLoaded", () => { setupNavigationToggle() }), document.addEventListener("DOMContentLoaded", () => { document.body.classList.contains("home") ? loadHomepageRecipes() : document.body.classList.contains("recipes") && loadAllRecipes() });